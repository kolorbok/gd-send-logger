# v2.0.29

- Fix the custom Requests star button across High / Medium / Low texture quality.
- Scale the raw PNG against an existing vanilla button instead of using a fixed 0.77 scale.
- Position the Requests button from the existing right-side button spacing and do not rerun the vanilla menu layout, so adding the mod button cannot move the buttons above it.
- Reject icon remains 1.25x. No request/filter/feedback behavior changed.
