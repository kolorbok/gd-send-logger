# v2.0.32

- Request list cells now show a small `REQ #...` identifier.
- Added a request info (+) button beside VIEW with description, review/feedback requirement, review language, and video availability.
- Added a YouTube button beside VIEW when a request has a video URL.
- Extended `/api/v1/requests` with escaped request metadata while keeping older clients compatible.
